<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DashTescam - Tesla Dashcam Visualizer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #000; color: #fff; }
        .tesla-red { color: #E82127; }
        .border-dash { border: 2px dashed #333; }
    </style>
</head>
<body class="font-sans">
    <nav class="p-6 flex justify-between items-center border-b border-gray-800">
        <h1 class="text-2xl font-bold tracking-tighter">DASH<span class="tesla-red">TESCAM</span></h1>
    </nav>

    <main class="max-w-4xl mx-auto pt-20 px-6 text-center">
        <h2 class="text-5xl font-extrabold mb-6">Maak je Tesla beelden <span class="text-gray-500">cinematic.</span></h2>
        
        <div id="upload-zone" class="border-dash rounded-3xl p-12 bg-gray-900/50 hover:border-red-600 transition-colors cursor-pointer mt-10">
            <div class="flex flex-col items-center">
                <div class="w-16 h-16 bg-gray-800 rounded-full flex items-center justify-center mb-4">
                    <span class="text-2xl text-red-600">+</span>
                </div>
                <p class="text-lg font-semibold">Sleep je Tesla .mp4 hierheen</p>
                <input type="file" id="fileInput" class="hidden" accept="video/*">
            </div>
        </div>

        <div id="preview-container" class="mt-10 hidden">
            <video id="videoPreview" controls class="w-full rounded-lg shadow-2xl"></video>
            <button class="mt-6 bg-red-600 text-white px-8 py-3 rounded-full font-bold hover:bg-red-700">Render met Telemetrie</button>
        </div>
    </main>

    <script src="script.js"></script>
</body>
</html>
